#!/usr/bin/env python

"""
Use the '.pyw' file extension to activate window mode and minimize output.
Declare the system path to the python binary in shebang statement,
and finally start the main loop through the file import.
"""

import heGUI
